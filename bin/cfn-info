#! /usr/bin/env ruby

require 'aws-sdk'
require 'yaml'

stack_name = ARGV.first
cfn = Aws::CloudFormation::Client.new
stack = cfn.describe_stacks(stack_name: stack_name).stacks.first

puts YAML.dump(stack.to_h)
